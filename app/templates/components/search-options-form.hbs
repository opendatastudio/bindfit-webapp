<div class="form-group">
    {{#each optionsParamsLabelled as |param|}}
        <div class="col-xs-12">
            <!-- If more than one sub-param, guess/fit only the first one -->
            {{#if (is-array param.label)}}
                <label>{{param.label.[0]}}</label>
            {{else}}
                <label>{{param.label}}</label>
            {{/if}}
            <div class="input-group">
                <span class="input-group-addon">Min</span>
                {{input value=param.value.min 
                        type="number" 
                        class="form-control"
                        key-up="onOptionsParamsChange"}}
                <span class="input-group-addon">Max</span>
                {{input value=param.value.max 
                        type="number" 
                        class="form-control"
                        key-up="onOptionsParamsChange"}}
            </div>
        </div>
    {{/each}}
</div>
<div class="form-group">
    <div class="col-xs-12">
        <label>Options</label>
        <!-- Selectors -->
        {{#if searchOptions.options._flavourList}}
            <div class="input-group">
                <span class="input-group-addon">Flavour</span>
                {{#power-select-multiple
                    options=searchOptions.options._flavourList
                    selected=searchOptions.options._flavourSelection
                    placeholder="Select flavours ..."
                    onchange=(action "onOptionsFlavourSelect")
                    as |flavour|}}
                    {{flavour.name}}
                {{/power-select-multiple}}
            </div>
        {{/if}}
        {{#if searchOptions.options._methodList}}
            <div class="input-group">
                <span class="input-group-addon">Method</span>
                {{#power-select-multiple
                    options=searchOptions.options._methodList
                    selected=searchOptions.options._methodSelection
                    placeholder="Select fit methods ..."
                    onchange=(action "onOptionsMethodSelect")
                    as |flavour|}}
                    {{flavour.name}}
                {{/power-select-multiple}}
            </div>
        {{/if}}

        <br/>

        <!-- Flags -->
        <!--
        <label>
            <div class="input-group">
                <span class="input-group-addon">
                    {{input checked=searchOptions.options.dilute type="checkbox"}}
                </span>
                <span class="input-group-addon" style="width: 100%">Dilution correction (if applicable)</span>
            </div>
        </label>
        <label>
            <div class="input-group">
                <span class="input-group-addon">
                    {{input checked=searchOptions.options.normalise type="checkbox"}}
                </span>
                <span class="input-group-addon" style="width: 100%">Subtract initial values</span>
            </div>
        </label>
        -->
    </div>
</div>
