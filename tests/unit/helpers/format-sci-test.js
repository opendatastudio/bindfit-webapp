import { formatSci } from '../../../helpers/format-sci';
import { module, test } from 'qunit';

module('Unit | Helper | format sci');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatSci(42);
  assert.ok(result);
});
