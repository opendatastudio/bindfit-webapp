import fitData from '../../../utils/fit-data';
import { module, test } from 'qunit';

module('Unit | Utility | fit data');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fitData();
  assert.ok(result);
});
